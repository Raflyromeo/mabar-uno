// URL server utama (Railway) — dipakai buat koneksi WebSocket utama
VITE_SERVER_URL=https://your-app.up.railway.app

// URL server cadangan (Vercel) — dipakai kalau server utama lagi error / gak bisa diakses
VITE_SERVER_URL_FALLBACK=https://your-server-url.vercel.app
